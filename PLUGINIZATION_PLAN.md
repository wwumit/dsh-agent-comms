# dsh-agent-comms 插件化规划 —— 智能体互联网的治理协议层

> 状态：规划稿 v0.1
> 日期：2026-08-20
> 定位：**智能体互联网（Agent Internet）的基础协议层**——不是普通工具插件
> 关联：CHA2A（信任执行）、GB/Z 185（身份码/身份管理/发现/交互/工具调用）、#3223（ToolRuntime 结算锚点）、A2A/MCP（协议层）

---

## 一、定位：为什么是"基础协议"

智能体互联网的协议栈分三层：

```
应用层     智能体应用 / 工作流
协议层     A2A（智能体间）· MCP（工具层）· 本系列（治理层：信任/披露/合规/审计）★
承载层     DID/CHA2A（身份）· HTTP/传输 · registry（信任执行）
```

- **A2A/MCP 定义"怎么通信"**（格式/端点），**本系列定义"通信可信吗、合规吗、可审计吗"**——是协议栈里缺的治理层
- 呼应 **GB/Z 185《人工智能 智能体互联》**：身份码/身份管理/能力描述/发现/交互/工具调用——本系列把治理机制落到可执行层
- 呼应 **CHA2A**：registry 是信任执行载体，本系列插件是协议消费者/执行者
- 呼应 **#3223**：ToolRuntime 结算锚点语义归 CHA2A §4.7，本系列插件是锚点语义的运行时实现

## 二、为什么做成插件（不是技能）

| | 技能（现状） | 插件（目标） |
|---|---|---|
| 触发 | agent 按需调用 | **运行时自动执行**（hook 进通信/工具调用链） |
| 生态位 | 内容层 | **协议执行层**（cordis 运行时成员） |
| 扫描 | 结构验证 | **三维扫描**（dshlib-scan/GuardDog/OSV）✅ |
| 发现 | git 技能目录 | **dsh-plugin topic + npm + 商店**（生态主流） |
| 协议性 | 检查工具 | **基础协议组件**（每次智能体通信都经过） |

**核心**：作为基础协议，治理不能靠"用户记得调技能"——必须**在通信路径上自动执行**（插件 hook），这正是插件形态的价值。

## 三、插件化架构（分层）

```
协议词汇层（不变）      check(ACC) · disclosure(ACD) · trust-probe(ATP) 规则/词汇
                       —— 对齐 CHA2A §4.7 / GB/Z 185，词汇单一真源
        ↓
插件执行层（新增）      cordis bundle 插件，把检查变成运行时能力
                       host 侧 hook：通信前/工具调用前/成功发布前
        ↓
信任支撑               CHA2A registry（在线核验：DID/等级/撤销）
        ↓
分发                  npm + git + dsh-plugin topic + dshlib 商店
```

**三个插件的运行时职责**（规划）：
| 插件 | 运行时职责 | hook 点 |
|---|---|---|
| **agent-trust-probe**（试点） | 通信/工具调用前验证信任锚（DID 注册/等级/撤销）→ 拒绝不可信 | `tools/pre-execute` + `tools/post-execute`（#3223 锚点语义） |
| **agent-comms-check** | 通信配置合规运行时检查（端点/认证/披露） | 通信建立前 |
| **agent-comms-disclosure** | 通信披露自动生成/校验（发什么/去哪/怎么存） | 通信请求/响应 |

## 四、试点：agent-trust-probe 插件（第一步）

**目标**：跑通 cordis bundle 插件形态，实现"通信前信任核验"最小闭环。

- **形态**：npm 包 + `dsh.bundle` + `cordis.patch.yml` + JS 入口（host 插件）
- **行为**：注册 `tools/pre-execute` hook → 调用 CHA2A registry 验证目标 DID（在线模式复用）→ 不可信则拒绝
- **验收**：在 DSH profile 里安装插件 → 触发工具调用 → 信任核验生效（可信放行/不可信拦截）
- **对齐**：#3223 zoahdev 指出的生命周期 seam（pre-execute 是 allow/deny gate）——我们正好用它

## 五、路线图

| 阶段 | 内容 | 验收 |
|---|---|---|
| **A 试点**（现在） | trust-probe 插件（cordis bundle + CHA2A 核验 hook） | 安装 → 信任核验生效 |
| **B 扩展** | check/disclosure 插件化 | 三插件齐全，通信治理闭环 |
| **C 协议化** | 与 #3223 锚点 + CHA2A §4.7 对齐，形成《智能体通信治理协议》 | 词汇/语义单一真源；生态引用 |
| **D 生态** | dsh-plugin topic + npm + 商店收录 + 生态目录 | 可被发现、被消费 |

## 六、与 GB/Z 185 的对应（国标依据）

国标定义"智能体互联"机制，本系列插件是这些机制的**运行时执行层**：

| 国标机制 | 本系列插件 | 关系 |
|---|---|---|
| 185.3 身份鉴别流程（依赖方→验证方→挑战→断言） | **trust-probe**：通信前调 CHA2A registry 验证信任锚 | 鉴别流程的自动执行 |
| 185.3 委托链验证（多级委托） | trust-probe ATP-002/003/007 | 委托链核验的运行时能力 |
| 185.4 能力描述（capabilities/skills） | **check**：通信配置合规检查 | 描述合规的运行时检查 |
| 185.5 发现（发现服务/预置信息） | 插件注册 + 商店 catalog + registry 解析 | 发现机制的信任侧 |
| 185.6 交互（请求/服务智能体；主从/代理协商/订阅） | **disclosure**：交互时披露生成/校验 | 交互的治理层 |
| 185.7 工具调用（工具列表获取/更新） | **锚点**：工具调用前/成功发布前 hook（#3223 §4.7） | 工具调用的信任执行 |
| 185.2 身份码/身份管理 | CHA2A registry（DID + nationalStandardId）| 插件依赖的信任底座 |

## 七、插件系列全景（智能体互联网治理协议）

> 治理协议分三层：**身份信任**（CHA2A/registry，已建）· **通信治理**（comm 系列，在建）· **行为治理**（生态系列，规划）——合起来覆盖国标 185 全机制。

### A. 通信治理（dsh-agent-comms，在建）
| 插件 | 状态 | 国标对应 |
|---|---|---|
| agent-trust-probe | 试点（下一个） | 185.3 鉴别 + 委托链 |
| agent-comms-check | 待插件化 | 185.4 能力描述合规 |
| agent-comms-disclosure | 待插件化 | 185.6 交互披露 |
| agent-comms-audit | 规划 | 185.3 审计追溯 |

### B. 身份信任（CHA2A/registry，已建）
| 组件 | 状态 | 国标对应 |
|---|---|---|
| cha2a registry（DID 注册/解析/凭证/撤销）| ✅ 运行中 | 185.2/185.3 |
| did:cha2a 方法（W3C 注册中）| ✅ 规范 | 185.2 身份码映射 |
| badge/trust query 接口 | ✅ | 185.3 验证 |

### C. 行为治理（生态系列，规划——智能体运行行为协议）
| 插件 | 定位 | 国标对应 | 关系 |
|---|---|---|---|
| **agent-behavior-guard** | 工具调用行为门（越权/敏感操作拦截）| 185.7 工具调用 | 与 dshlib 扫描分级（安全信号/客观阻断）联动 |
| **agent-consent-gate** | 授权/同意门（敏感数据访问前确认）| 185.3 授权决策 | 与权限画像（AI-Scarlett 启发）呼应 |
| **agent-audit-trail** | 调用链/行为审计日志（证据契约到运行时）| 185.3 审计追溯 | 与 EVIDENCE 存档衔接 |
| **agent-capability-disclose** | 能力/权限披露（agent 能做什么，机器可读）| 185.4 能力描述 | 与 SKILL.md 能力声明衔接 |
| **agent-discovery-node** | 发现/解析节点（agent 目录服务）| 185.5 发现 | 与商店 catalog + registry 衔接 |
| **agent-interaction-contract** | 交互契约（主从/代理协商/订阅的模式校验）| 185.6 交互 | 与 A2A 协商对齐 |
| **agent-tool-registry** | 工具列表注册/更新/校验 | 185.7 工具调用 | 与 catalog/工具注册衔接 |

### D. 支撑（已建/在建）
| 组件 | 状态 |
|---|---|
| dshlib 商店（认证+扫描+报告）| ✅ 运行中 |
| CHA2A §4.7 运行时锚点词汇 | ✅ 规范预留 |
| #3223 结算锚点语义 | 待对齐（作者发布后）|

## 八、路线图（更新）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **A 试点**（现在）| trust-probe 插件（cordis bundle + CHA2A 核验 hook）| 安装 → 信任核验生效 |
| **B 扩展** | check/disclosure 插件化；audit 补上 | 通信治理四插件齐全 |
| **C 行为层** | behavior-guard / consent-gate 等（先 1-2 个试点）| 工具调用行为治理生效 |
| **D 协议化** | 与 #3223 + CHA2A §4.7 对齐，形成《智能体通信治理协议》| 词汇单一真源；生态引用 |
| **E 生态** | dsh-plugin topic + npm + 商店 + 生态目录 | 可被发现、被消费 |

## 九、风险与原则

- **技术栈**：技能是 Python，插件要 JS/TS（cordis 生态）——试点先验证可行性
- **不与官方冲突**：走官方支持的分发路径（bundle + patch），不发明新机制
- **协议不绑定实现**：词汇层独立（CHA2A 对齐），插件是参考实现——未来官方或他人可实现
- **诚实**：试点验证真实生命周期，不宣称"协议已标准化"

---

*内部规划；试点先跑通，再谈协议地位。*

## 十、形态分析（2026-08-20，慢下来后复盘）

### 10.1 现状：已提交的是"技能形态"
- dsh-agent-comms 商店条目 = 技能仓库（git 源，scan: clean，但 **scan_gd: none**——GuardDog 扫 npm 包，git 技能扫不了）
- 三个技能（check/disclosure/trust-probe）是**内容层工具**：用户/agent 主动调用才执行

### 10.2 形态错位：技能 vs 基础协议定位
- **技能** = 主动调用（检查工具）
- **基础协议** = 运行时自动执行（每次通信/工具调用都经过）
- 用户定位"智能体互联网基础协议" → 需要**插件形态**（hook 进执行路径），当前技能形态未达

### 10.3 技能形态的价值（不白做）
1. **规则/词汇沉淀**：ACC/ACD/ATP 规则集 = 协议词汇载体（对齐 CHA2A/GBZ185），形态无关，永久有效
2. **在线核验已实现**：trust-probe --verify-registry（调 CHA2A）证明核验逻辑
3. **平台能力修复**：3 个 bug（技能仓库识别/github 写库/去重）+ registry update 接口——与形态无关，永久有效
4. **CHA2A 升级验证**：L1→L3 update 机制验证通过（凭证生命周期补全）

### 10.4 关键判断
- 插件化 = "协议词汇换执行壳"，不是从零做
- 技能形态不撤销（独立价值：工具层 + 对齐展示）
- 是否双形态共存（技能=工具层，插件=运行时层）待定——**决定要不要做插件**
